# Claude Code Configuration

Custom slash commands, skills, and agents for Claude Code.

## Setup

Symlink the directories to make them available globally:

```bash
mkdir -p ~/.claude
ln -sf "$(pwd)/commands" ~/.claude/commands
ln -sf "$(pwd)/skills" ~/.claude/skills
ln -sf "$(pwd)/agents" ~/.claude/agents
```
